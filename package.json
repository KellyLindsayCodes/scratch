{
  "dependencies": {
    "gh-pages": "^5.0.0",
    "gsap": "^3.12.1",
    "three": "^0.153.0"
  },
  "devDependencies": {
    "vite": "^4.3.9"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "vite build && gh-pages -d dist"
  }
}
